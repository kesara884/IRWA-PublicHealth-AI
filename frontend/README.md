# PublicHealth-AI Frontend

React + Vite chat/dashboard shell for the mid-evaluation demo.

## Quick start (Phase 1)

```bash
cd frontend
npm install
npm run dev
```

Open [http://localhost:5173](http://localhost:5173). Ensure the backend is running so the Dashboard health check succeeds.
